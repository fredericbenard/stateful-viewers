# Experiment: Style generation (text-only)
#
# Tests v2 reflective style generation across 7 variability hints.
# No images — text-only LLM calls.

experiment_id: style_generation

provider: openai
model: gpt-5.2

temperature: 0.95
max_tokens: 1024

# No images: text-only experiment.
images: []

prompt_variant_ids: []
