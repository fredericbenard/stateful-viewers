# Example environment variables (no secrets).
#
# These are only needed if you run the offline scripts in `scripts/` that call LLM APIs
# (e.g. generate public artifacts, retrofit labels). They are NOT required to run the app.
#
# Copy to `.env` and fill in your own keys if needed:
#   cp .env.example .env
#
# OpenAI (for --llm openai in scripts)
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
#
# Google Gemini (for --llm gemini in scripts)
# Get your key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=
#
# Anthropic (for --llm anthropic in scripts)
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

